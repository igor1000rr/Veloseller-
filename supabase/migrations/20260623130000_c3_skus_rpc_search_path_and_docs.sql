-- ============================================================================
-- C3 (часть 2) — get_skus_period_metrics: search_path + документация. 2026-06-23
-- Применено на prod через MCP; здесь — для паритета репозитория.
--
-- Валидация (2 продавца, 30-дн окно, по 50 SKU): RPC совпадает с канонической
-- confirmed_velocity воркера 1:1 (in_stock/stockout/coverage/lost_revenue — 0 diff,
-- velocity — 0 diff vs confirmed). От ОТОБРАЖАЕМОЙ adjusted_velocity отличается лишь
-- для ~5% SKU с аномалиями/пополнениями на ~0.005 ед/день (медианная коррекция —
-- 30-дневная база, к произвольному/предпраздничному окну неприменима). Признано
-- незначимым и ожидаемым → переписывать RPC под adjusted НЕ требуется.
-- Высоковидимое расхождение C3.1 (KPI дашборда) исправлено отдельно: плитки читают
-- каноническую warehouse_metrics (lib/dashboard-cache.ts).
-- ============================================================================

ALTER FUNCTION public.get_skus_period_metrics(uuid, uuid, date, date, uuid[])
  SET search_path = public;

COMMENT ON FUNCTION public.get_skus_period_metrics(uuid, uuid, date, date, uuid[]) IS
'Метрики SKU за ПРОИЗВОЛЬНЫЙ период (кастомный диапазон / предпраздничное окно), которые воркер не пересчитывает заранее. Возвращает CONFIRMED-скорость (sales_like / дни-в-наличии). Валидация 23.06.2026: совпадает с confirmed_velocity воркера 1:1; от adjusted_velocity отличается лишь для ~5% SKU (аномалии/пополнения) на ~0.005 ед/день — незначимо. Высоковидимое расхождение KPI дашборда (C3.1) исправлено отдельно: плитки читают warehouse_metrics.';
